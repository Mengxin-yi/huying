# HuYing 深度重构设计文档

> 日期：2026-04-28
> 源项目:huying_board（FirstUI）
> 目标项目：huying（uview-plus）
> 重构程度：深度重构（UI 全部重新设计）

---

## 1. 重构概述

将源项目 huying_board 的全部业务逻辑迁移至新项目 huying，同时：

- **UI 库全面替换**：FirstUI → uview-plus，源项目所有外部 UI 库和组件依赖全部移除
- **UI 全部重新设计**：简洁现代风格，圆角卡片式布局，轻阴影，微交互动画
- **登录模块完整重做**：支持账号密码 + 手机验证码两种登录方式
- **引入 Pinia 状态管理**：统一管理全局状态，多端兼容
- **API 接口不变**：保持 `/cpi/`、`/bpi/`、`/wpi/` 三套接口前缀
- **平台优先级**：H5 > App > 微信小程序

### 迁移范围

全部 5 个模块，预计 35-38 个页面（源项目 44 页，合并精简部分功能相似页面）：

| 模块 | 说明 |
|------|------|
| 今日事 | 任务管理、日历视图 |
| 微逛 | 内容浏览、搜索 |
| 订阅 | 订阅管理 |
| 发布 | 内容发布 |
| 我的 | 个人中心、设置 |

### 源项目页面映射表

源项目 44 页 → 目标项目 35 页，部分页面合并精简：

**登录模块（3 → 3 页）**

| 源页面 | 目标页面 | 处理 |
|--------|---------|------|
| login.nvue | pages/login/index.vue | 重写 |
| pages/prefer/user/signup | pages/login/register.vue | 迁移 |
| pages/prefer/user/recover | pages/login/forgot.vue | 迁移 |

**今日事模块（4 → 4 页）**

| 源页面 | 目标页面 | 处理 |
|--------|---------|------|
| pages/note/list | pages/index/index.vue | 重写 |
| pages/note/detail | pages/index/detail.vue | 迁移 |
| pages/note/add | pages/index/add.vue | 迁移 |
| _(新增)_ | pages/index/calendar.vue | 新增页面 |

**微逛模块（2 → 3 页）**

| 源页面 | 目标页面 | 处理 |
|--------|---------|------|
| pages/weiguang/list | pages/weiguang/index.vue | 重写 |
| pages/weiguang/detail | pages/weiguang/detail.vue | 迁移 |
| _(新增)_ | pages/weiguang/search.vue | 新增页面 |

**订阅模块（4 → 3 页）**

| 源页面 | 目标页面 | 处理 |
|--------|---------|------|
| pages/order/index | pages/subscribe/index.vue | 重写 |
| pages/order/add | pages/subscribe/add.vue | 迁移 |
| pages/order/kuozeng | pages/subscribe/detail.vue | 合并（扩增与详情合并） |

**发布模块（5 → 3 页）**

| 源页面 | 目标页面 | 处理 |
|--------|---------|------|
| pages/publish/index | pages/publish/index.vue | 重写 |
| pages/publish/add + pages/publish/add2 | pages/publish/editor.vue | 合并（两种发布合并为一个编辑器） |
| pages/publish/leo-list | pages/publish/preview.vue | 迁移改造 |

**移除的发布页面**：
- `pages/publish/pllist`（评论列表）→ 功能合并到详情页，不单独设页
- `pages/publish/publish-item` → 合并到 editor.vue

**我的模块（20 → 12 页）**

| 源页面 | 目标页面 | 处理 |
|--------|---------|------|
| pages/prefer/index | pages/prefer/index.vue | 重写 |
| pages/prefer/user/profile + pages/prefer/user/editeruser + pages/prefer/user/upimguser | pages/prefer/profile.vue | 合并（个人资料编辑统一） |
| pages/prefer/vip/inside | pages/prefer/vip.vue | 迁移 |
| pages/prefer/vip/bill | pages/prefer/order.vue | 迁移 |
| pages/prefer/account + pages/prefer/secure/index | pages/prefer/settings.vue | 合并（设置统一） |
| pages/note/about | pages/prefer/about.vue | 迁移 |
| pages/prefer/feedback | pages/prefer/feedback.vue | 迁移 |
| pages/prefer/auth + pages/prefer/authentication + pages/prefer/authenticationb | pages/prefer/company.vue | 合并（认证流程统一） |
| pages/prefer/namecard | pages/prefer/company.vue | 合并（名片归入企业认证） |
| pages/prefer/contact | pages/prefer/about.vue | 合并（联系方式归入关于页） |
| pages/prefer/user/changepassword | pages/prefer/settings.vue | 合并（修改密码归入设置） |
| pages/prefer/secure/mobile + pages/prefer/secure/bindwechat | pages/prefer/settings.vue | 合并（安全设置统一） |

**移除的"我的"页面**：
- `pages/prefer/educationAdd`（新增学历）→ 暂不迁移，后续按需加
- `pages/prefer/skillAdd`（新增技能）→ 暂不迁移
- `pages/prefer/addxl`（添加简历项）→ 暂不迁移
- `pages/prefer/followed`（我的关注）→ 暂不迁移
- `pages/prefer/privacy`（隐私协议）→ 内嵌为弹窗组件，不单独设页
- `pages/prefer/user/policy`（用户协议）→ 内嵌为弹窗组件
- `pages/prefer/vip/invoice`（申请开票）→ 暂不迁移
- `pages/prefer/promote/coupon`（卡券）→ 暂不迁移
- `pages/prefer/help/*`（帮助页面：helpbid、helpreserve、wechatnotice、helpua）→ 暂不迁移

**客服模块（2 → 1 页）**

| 源页面 | 目标页面 | 处理 |
|--------|---------|------|
| pages/kefu/index | pages/kefu/index.vue | 迁移 |
| pages/kefu/userIndex | _移除_ | 合并到主客服页 |

**总计**：源 44 页 → 目标 35 页（含 2 个新增页面：calendar、search）

---

## 2. 项目目录结构

```
huying/
├── api/                        # API 接口层
│   ├── request.js              # 请求封装（/cpi/, /bpi/, /wpi/）
│   ├── modules/                # 按模块拆分的接口定义
│   │   ├── user.js             # 用户相关接口
│   │   ├── task.js             # 今日事接口
│   │   ├── social.js           # 微逛接口
│   │   ├── subscribe.js        # 订阅接口
│   │   ├── publish.js          # 发布接口
│   │   └── common.js           # 公共接口
│   └── index.js                # 统一导出
├── store/                      # Pinia 状态管理
│   ├── index.js
│   └── modules/
│       ├── user.js             # 用户信息 & 登录状态
│       ├── app.js              # 全局应用状态
│       └── subscribe.js        # 订阅相关状态
├── common/
│   ├── js/
│   │   ├── utils/              # 工具函数
│   │   │   ├── common.js       # 通用工具（日期、GUID 等）
│   │   │   ├── auth.js         # Token 管理
│   │   │   └── page.js         # 页面导航工具
│   │   └── constants.js        # 常量定义
│   └── css/
│       ├── base.css            # 基础样式
│       └── theme.css           # 主题变量
├── components/                 # 全局公共组件
│   ├── PageEmpty.vue           # 空状态
│   ├── PageLoading.vue         # 加载状态
│   ├── Calendar/               # 日历组件
│   │   └── index.vue
│   └── Timeline/               # 时间轴组件
│       └── index.vue
├── pages/                      # 页面
│   ├── login/                  # 登录模块
│   │   ├── index.vue          # 登录页
│   │   ├── register.vue        # 注册页
│   │   └── forgot.vue          # 忘记密码
│   ├── index/                  # 今日事（首页）
│   │   ├── index.vue
│   │   ├── detail.vue
│   │   ├── add.vue
│   │   ├── calendar.vue
│   │   ├── hooks/
│   │   │   └── useTaskList.js  # 任务列表逻辑
│   │   └── components/
│   │       ├── TaskCard.vue
│   │       └── WeekSelector.vue
│   ├── weiguang/               # 微逛
│   │   ├── index.vue
│   │   ├── detail.vue
│   │   ├── search.vue          # 新增页面
│   │   ├── hooks/
│   │   │   └── useArticleList.js
│   │   └── components/
│   │       └── ArticleCard.vue
│   ├── subscribe/              # 订阅
│   │   ├── index.vue
│   │   ├── add.vue
│   │   └── detail.vue
│   ├── publish/                # 发布
│   │   ├── index.vue
│   │   ├── editor.vue
│   │   └── preview.vue
│   ├── prefer/                 # 我的
│   │   ├── index.vue
│   │   ├── profile.vue
│   │   ├── vip.vue
│   │   ├── settings.vue
│   │   ├── about.vue
│   │   ├── feedback.vue
│   │   ├── company.vue
│   │   └── order.vue
│   └── kefu/                   # 客服
│       └── index.vue
├── App.vue                     # 应用入口
├── main.js                     # 主入口（注册 Pinia、uview-plus）
├── manifest.json               # 应用配置（AppID、平台、权限）
├── pages.json                  # 路由与导航配置
├── uni.scss                    # 全局 SCSS 变量
├── static/
│   ├── icon/                   # 图标（tabbar 等）
│   ├── image/                  # 图片资源
│   └── font/                   # iconfont 自定义图标
└── uni_modules/
    └── uview-plus/             # UI 组件库（已有）
```

---

## 3. 基础设施设计

### 3.1 请求封装 (api/request.js)

保留源项目三套接口前缀，统一封装为 Promise 风格：

- `/cpi/` — 业务接口（status_code: 200）
- `/bpi/` — 系统接口（status: 200）
- `/wpi/` — 数字人接口
- 自动 Token 管理（从 Pinia user store 读取）
- Token 过期自动刷新 + 请求重试
- 请求/响应拦截器（Loading、错误处理）
- 所有请求携带 `app-name: 'huying'` header

**接口签名设计**：

```javascript
// api/request.js — 工厂函数创建三套请求实例

/**
 * 创建请求实例
 * @param {string} prefix - 接口前缀（/cpi/, /bpi/, /wpi/）
 * @param {string} successKey - 成功状态字段名
 * @returns {Object} 请求方法集合
 */
function createRequest(prefix, successKey) {
  return {
    get: (url, params) => request('GET', prefix + url, params, successKey),
    post: (url, data) => request('POST', prefix + url, data, successKey),
    put: (url, data) => request('PUT', prefix + url, data, successKey),
    delete: (url) => request('DELETE', prefix + url, {}, successKey),
  }
}

export const cpi = createRequest('/cpi/', 'status_code')  // 业务接口
export const bpi = createRequest('/bpi/', 'status')       // 系统接口
export const wpi = createRequest('/wpi/', 'status_code')  // 数字人接口
```

### 3.1.1 错误处理策略

- **网络错误**（无响应）：显示 Toast 提示「网络异常，请检查网络连接」
- **401/403 未授权**：清除本地 Token → 跳转登录页（`uni.reLaunch`）
- **Token 过期**：自动使用 refreshToken 刷新，刷新成功重试原请求（最多重试 1 次）
- **刷新失败**：清除登录状态 → 跳转登录页
- **业务错误**（接口返回非成功状态）：显示后端返回的错误信息 Toast
- **请求超时**：默认 15 秒，超时显示 Toast「请求超时，请稍后重试」
- **重复请求**：同一接口在 pending 状态时忽略后续请求（防抖）

### 3.2 Pinia 状态管理

**多端兼容要点**：
- 持久化插件使用自定义 storage 适配器（`uni.setStorageSync` / `uni.getStorageSync`）
- Store 中禁止使用 `window`、`document` 等浏览器特有 API
- 每次涉及 Store 的修改后，验证 H5 + 小程序 + App 三端运行正常

**Pinia 初始化配置**：

```javascript
// store/index.js
import { createPinia } from 'pinia'
import { createPersistedState } from 'pinia-plugin-persistedstate'

const pinia = createPinia()

// 使用 uni-app 存储适配器（兼容 H5、小程序、App 三端）
pinia.use(createPersistedState({
  storage: {
    getItem: (key) => uni.getStorageSync(key),
    setItem: (key, value) => uni.setStorageSync(key, value),
    removeItem: (key) => uni.removeStorageSync(key),
  },
}))

export default pinia
```

**Store 模块**：

| 模块 | 职责 | 持久化 |
|------|------|--------|
| user | token、用户信息、登录状态 | 是 |
| app | 全局配置、系统信息 | 部分 |
| subscribe | 订阅列表、状态 | 否 |

### 3.3 路由配置

**TabBar（5 个 Tab）**：

| Tab | 名称 | 页面路径 |
|-----|------|---------|
| 1 | 今日事 | pages/index/index |
| 2 | 微逛 | pages/weiguang/index |
| 3 | 订阅 | pages/subscribe/index |
| 4 | 发布 | pages/publish/index |
| 5 | 我的 | pages/prefer/index |

**页面导航**：登录页不设为 TabBar 页，通过 `uni.reLaunch` 跳转。

---

## 4. 全局样式与设计规范

### 4.1 色彩体系

```
主题色:     #007AFF (蓝色)
辅助色:     #5AC8FA (浅蓝)
成功色:     #4CD964 (绿色)
警告色:     #F0AD4E (橙色)
危险色:     #DD524D (红色)
背景色:     #F5F6FA (浅灰)
卡片背景:   #FFFFFF
分割线:     #EAEAEA
文字主色:   #333333
文字次色:   #666666
文字弱色:   #999999
```

### 4.2 字体规范

```
特大标题:   20px / font-weight: 600
页面标题:   17px / font-weight: 600
正文:       14px / font-weight: 400
辅助文字:   12px / font-weight: 400
极小文字:   10px / font-weight: 400
```

### 4.3 间距规范

```
页面边距:   30rpx (两侧)
卡片圆角:   16rpx
按钮圆角:   12rpx
组件间距:   20rpx
模块间距:   30rpx
间距基准:   4rpx
  - xs: 8rpx
  - sm: 16rpx
  - md: 24rpx
  - lg: 32rpx
  - xl: 48rpx
```

### 4.4 设计风格

- 简洁现代、大量留白
- 圆角卡片式布局
- 轻阴影替代边框
- 图标：uview-plus 内置 + iconfont 自定义补充
- 微交互动画（按钮点击、页面切换）
- 所有尺寸使用 rpx 单位

### 4.5 全局 SCSS 变量 (uni.scss)

> 注意：全局样式统一使用 `uni.scss` 中的 SCSS 变量，`common/css/base.css` 仅放置基础 reset 样式，`common/css/theme.css` 不再重复定义颜色变量。

```scss
$primary-color: #007AFF;
$success-color: #4CD964;
$warning-color: #F0AD4E;
$danger-color: #DD524D;
$bg-color: #F5F6FA;
$card-bg: #FFFFFF;
$text-color: #333333;
$text-color-secondary: #666666;
$text-color-weak: #999999;
$border-color: #EAEAEA;
$page-padding: 30rpx;
$card-radius: 16rpx;
$btn-radius: 12rpx;
$spacing-xs: 8rpx;
$spacing-sm: 16rpx;
$spacing-md: 24rpx;
$spacing-lg: 32rpx;
$spacing-xl: 48rpx;
```

---

## 5. 登录模块设计

### 5.1 页面列表

| 页面 | 文件 | 说明 |
|------|------|------|
| 登录 | `pages/login/index.vue` | 单模式登录 |
| 注册 | `pages/login/register.vue` | 注册页 |
| 忘记密码 | `pages/login/forgot.vue` | 密码重置 |

### 5.2 登录页功能

**登录方式（Tab 切换）**：
1. 账号密码登录：用户名 + 密码 + 登录按钮
2. 手机验证码登录：手机号 + 验证码 + 获取验证码按钮 + 登录按钮

**附加功能**：
- 记住密码（账号密码模式）
- 忘记密码入口
- 用户协议 & 隐私政策勾选
- 注册入口

### 5.3 登录页布局

```
┌─────────────────────────┐
│                         │
│       [Logo/品牌图]      │
│       应用名称           │
│                         │
│  ┌─────────┬─────────┐  │
│  │ 账号登录 │ 验证码登录│  │  ← Tab 切换
│  └─────────┴─────────┘  │
│                         │
│  ┌───────────────────┐  │
│  │ 用户名/手机号      │  │
│  └───────────────────┘  │
│  ┌───────────────────┐  │
│  │ 密码/验证码  [获取]│  │
│  └───────────────────┘  │
│                         │
│  □ 记住密码  忘记密码?  │
│                         │
│  ┌───────────────────┐  │
│  │     立即登录       │  │
│  └───────────────────┘  │
│                         │
│  □ 已阅读并同意《用户协议》│
│                         │
│     还没有账号？去注册   │
│                         │
└─────────────────────────┘
```

### 5.4 技术要点

- 使用 uview-plus 组件：`u-input`、`u-button`、`u-tabs`
- Token 存入 Pinia user store（持久化到 uni.setStorageSync）
- 登录成功后 `uni.reLaunch` 跳转首页

---

## 6. 各模块核心功能

### 6.1 今日事（任务管理）

**首页 — 任务列表**：
- 顶部：横向滑动日期选择器（一周）
- 统计卡片：今日任务数 / 已完成 / 待处理
- 任务列表：卡片式，显示标题、优先级标签、截止时间
- 左滑操作：完成 / 推迟 / 删除
- 右下角 FAB 按钮：新增任务
- 下拉刷新 + 上拉加载更多

**任务详情**：标题、描述、优先级、截止日期、子任务清单

**日历视图**（`calendar.vue`，独立路由页面）：
- 从任务列表页点击日历图标进入
- 月历展示，有任务日期显示标记点
- 点击日期筛选当天任务列表
- 可直接从日历页跳转到新增任务页

### 6.2 微逛（内容浏览）

**首页**：搜索入口 + 分类 Tab + 信息流卡片（封面图 + 标题 + 摘要 + 来源 + 时间）
**详情**：富文本正文 + 作者信息 + 相关推荐

### 6.3 订阅管理

**首页**：订阅卡片列表（名称、状态标签、到期时间）
**添加/编辑**：订阅信息表单

### 6.4 发布

**首页**：已发布内容列表（状态标签：草稿/已发布/审核中）
**编辑器**：标题 + 富文本编辑 + 封面图上传 + 分类选择

### 6.5 我的（个人中心）

**首页 — 个人中心**：
- 顶部用户卡片：头像、昵称、VIP 状态标签、编辑按钮
- 功能宫格：企业认证、订单管理
- 功能列表：意见反馈、设置、关于、联系客服

**profile.vue — 个人资料**（合并源项目：修改用户名、修改头像、个人资料）：
- 头像修改（拍照/相册）
- 基本信息：昵称、手机号、邮箱
- 个人简介

**vip.vue — VIP 会员**（迁移源项目 vip/inside）：
- 当前会员等级、到期时间
- 会员权益说明
- 续费/开通入口

**order.vue — 订单管理**（迁移源项目 vip/bill）：
- 订单列表（全部/待付款/已完成）
- 订单详情

**settings.vue — 设置**（合并源项目：account、secure/index、secure/mobile、secure/bindwechat、user/changepassword）：
- 账号安全：修改密码、绑定手机、绑定微信
- 通用设置：消息通知、推送设置
- 清除缓存
- 退出登录

**about.vue — 关于**（合并源项目：note/about、prefer/contact）：
- 应用版本信息
- 联系我们
- 用户协议（内嵌弹窗）
- 隐私政策（内嵌弹窗）

**feedback.vue — 意见反馈**（迁移源项目 prefer/feedback）：
- 反馈类型选择
- 文本输入 + 图片上传
- 提交反馈

**company.vue — 企业认证**（合并源项目：prefer/auth、prefer/authentication、prefer/authenticationb、prefer/namecard）：
- 认证状态展示
- 认证流程引导
- 企业名片展示

### 6.6 客服模块

**kefu/index.vue — 客服**（合并源项目：kefu/index、kefu/userIndex）：
- 在线聊天界面
- 常见问题快捷入口
- 转人工客服

---

## 7. 代码分层架构

### 7.1 分层结构

```
┌─────────────────────────────────┐
│           Pages (页面层)          │  职责：布局、UI 渲染、事件绑定
├─────────────────────────────────┤
│     Components (组件层)          │  职责：UI 展示、props/emit 交互
├─────────────────────────────────┤
│       Hooks (逻辑复用层)         │  职责：页面级业务逻辑封装
├─────────────────────────────────┤
│        Store (状态管理层)        │  职责：全局共享状态、跨页面数据
├─────────────────────────────────┤
│         API (接口层)             │  职责：HTTP 请求、数据转换
├─────────────────────────────────┤
│       Utils (工具层)             │  职责：纯函数、无副作用工具
└─────────────────────────────────┘
```

### 7.2 数据流向

```
Page → 调用 Hook → Hook 调用 API → API 返回数据 → Hook 更新 ref → 页面响应式渲染
                                 ↓
                          需要全局共享？→ 写入 Store
```

### 7.3 各层规范

**Pages**：最薄的一层，只做组合，不写业务逻辑
**Hooks**：核心业务逻辑，命名 `useXxxList.js`，导出 `export function useXxxList()`
**API**：只负责请求，命名 `getXxx / postXxx`
**Store**：只放全局共享数据（token、用户信息），页面临时数据放 hooks
**Utils**：纯函数，无副作用
**Components**：无业务逻辑，不直接调用 API 或操作 Store

---

## 8. 编码规范

### 8.1 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 页面目录 | kebab-case | `pages/weiguang/detail` |
| 页面文件 | TabBar入口用 index.vue，子页面用语义化命名 | `pages/login/index.vue`、`detail.vue`、`add.vue` |
| 组件文件 | PascalCase | `TaskCard.vue` |
| 组件目录 | PascalCase，复杂组件用文件夹 | `components/Calendar/index.vue` |
| JS 工具文件 | camelCase | `common/js/utils/formatDate.js` |
| API 模块 | camelCase | `api/modules/task.js` |
| Store 模块 | camelCase | `store/modules/user.js` |
| 静态资源 | kebab-case | `static/icon/tab-task.png` |
| CSS 类名 | BEM | `.task-card__title--active` |

### 8.2 变量/函数命名

```javascript
// boolean: is/has/should 前缀
const isLoading = ref(false)
// 数组: xxxList
const taskList = ref([])
// 对象: xxxInfo
const userInfo = ref({})
// 常量: 全大写 + 下划线
const MAX_PAGE_SIZE = 20
// 函数: fetch/get/post + 动词
const fetchTaskList = async () => {}
// 事件: handleXxx
const handleTabChange = () => {}
// 工具: 动词开头
const formatTime = () => {}
// 初始化: initXxx
const initPage = () => {}
```

### 8.3 组件规范

```
组件结构顺序：<template> → <script setup> → <style lang="scss" scoped>
Props：defineProps，声明 type + default + required
Emits：defineEmits，动词开头（update, delete, change, submit）
组件分类：
  - components/         ← 全局公共组件（跨模块复用）
  - pages/xxx/components/ ← 页面级私有组件（仅该模块使用）
```

### 8.4 注释规范（强制）

**所有变量、常量、函数、参数定义时必须加中文注释说明用途**：

```javascript
// 变量：行尾注释
const isLoading = ref(false) // 页面加载状态
const taskList = ref([]) // 任务列表数据
const currentPage = ref(1) // 当前页码

// 常量：行尾注释
const MAX_PAGE_SIZE = 20 // 每页最大条数
const TOKEN_KEY = 'huying_token' // Token 存储键名

// 函数：JSDoc 注释
/**
 * 获取任务列表
 * @param {boolean} refresh - 是否刷新（重置页码）
 */
const fetchTaskList = async (refresh = false) => {}

/**
 * 处理Tab切换
 * @param {number} index - 选中的Tab索引
 */
const handleTabChange = (index) => {}

// API 函数：JSDoc + 参数说明
/**
 * 用户登录
 * @param {Object} data - 登录参数
 * @param {string} data.username - 用户名
 * @param {string} data.password - 密码
 */
export const login = (data) => request.post('/bpi/login', data)

// Props：行尾注释
const props = defineProps({
  title: { type: String, default: '' }, // 卡片标题
  showArrow: { type: Boolean, default: false } // 是否显示箭头
})

// Store state：行尾注释
state: () => ({
  token: '', // 用户登录凭证
  userInfo: null, // 用户基本信息
  isLoggedIn: false // 登录状态
})
```

### 8.5 文件模板

**页面模板**：
```vue
<template>
  <view class="page">
    <!-- 页面内容 -->
  </view>
</template>

<script setup>
import { ref, onMounted } from 'vue'

// --- 状态定义 ---

// --- 方法定义 ---

// --- 生命周期 ---
onMounted(() => {
  initPage()
})
</script>

<style lang="scss" scoped>
.page {
  min-height: 100vh;
  background-color: $bg-color;
}
</style>
```

**组件模板**：
```vue
<template>
  <view class="component-name">
    <!-- 组件内容 -->
  </view>
</template>

<script setup>
const props = defineProps({
  title: { type: String, default: '' }, // 标题
})

const emit = defineEmits(['click', 'change'])
</script>

<style lang="scss" scoped>
.component-name {
  // 样式
}
</style>
```

### 8.6 模块内部结构

```
pages/weiguang/
├── index.vue              # 模块首页
├── detail.vue             # 子页面
├── components/            # 模块私有组件
│   └── ArticleCard.vue
└── hooks/                 # 模块私有逻辑
    └── useArticleList.js
```

---

## 9. 开发阶段与交付

### 阶段 1：基础设施搭建
- 安装 Pinia + 持久化插件（uni-app 适配）
- 请求封装（`/cpi/`, `/bpi/`, `/wpi/`）
- 全局样式 & 主题变量 & uni.scss
- 路由配置（pages.json + TabBar 图标）
- 工具函数迁移（common.js、page.js、auth.js）

### 阶段 2：登录模块
- login.vue（账号密码登录）
- 注册页
- 忘记密码页
- Pinia user store
- Token 自动续期逻辑

### 阶段 3：今日事模块
- 任务列表页（日期选择器、统计卡片、滑动操作）
- 任务详情页
- 新增/编辑任务页
- 日历视图
- 公共组件：Calendar、Timeline

### 阶段 4：微逛模块
- 内容列表页（分类 Tab、搜索）
- 内容详情页

### 阶段 5：订阅模块
- 订阅列表页
- 添加/编辑订阅页
- 订阅详情页

### 阶段 6：发布模块
- 发布列表页
- 内容编辑器
- 预览页

### 阶段 7：我的模块
- 个人中心首页
- 个人信息编辑
- VIP 会员页
- 设置、关于、反馈、企业认证、订单管理

### 每阶段交付标准

1. 功能完整可用
2. H5 端主要验证通过；App 和小程序端对 Store 和导航进行抽查
3. 无 console 错误/警告
4. UI 符合设计规范
5. API 调用正确（对接现有后端）
6. 所有变量/函数/参数有中文注释

---

## 10. 技术栈总结

| 项 | 选择 |
|----|------|
| 框架 | uni-app (Vue 3) |
| UI 库 | uview-plus（唯一 UI 库） |
| 图标 | uview-plus 内置 + iconfont 自定义 |
| 状态管理 | Pinia + pinia-plugin-persistedstate |
| HTTP 请求 | uni.request 封装 |
| 日期处理 | dayjs |
| CSS 预处理 | SCSS |
| 主题色 | #007AFF |
| 平台优先 | H5 > App > 微信小程序 |
